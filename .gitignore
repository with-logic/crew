node_modules/
dist/
coverage/
bun.lockb
.DS_Store
*.log
# Stale bun --compile artifacts left by an aborted build (e.g. when the
# atomic rename over process.execPath fails mid-flight).
.*.bun-build
# Claude Code runtime files.
.claude/

# Local release-signing private key. The public key is committed in
# src/self-update/signing-key.ts and site/public/install.sh.
.crew-release-signing-key.pem
